# Optional. When both are set, the app uses Supabase Auth.
# When absent (the default for this demo), it falls back to Demo Auth automatically.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
